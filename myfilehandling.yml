---
- hosts: 10.0.0.5
  become: yes
  tasks: 
  - include_tasks: myfile.yml
  - name: install apache2
    apt:
      name: apache2
  - name: copy a file into node 
    copy:
       src: myindexfile
       dest: /home/azureuser